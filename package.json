{
  "name": "ball-thrower-server",
  "version": "0.0.0",
  "description": "RESTful API for controlling a ball thrower robot.",
  "main": "index.js",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "start": "tsc && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guilherme Agostinelli",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.17",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  }
}

{
  "name": "ball-thrower-server",
  "version": "1.2.2",
  "description": "RESTful API for controlling a ball thrower robot.",
  "main": "dist/app.js",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "npm run lint && tsc",
    "start": "node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guilherme Agostinelli",
  "repository": {
    "type": "git",
    "url": "https://github.com/guilhermeagostinelli/ball-thrower-server.git"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.17",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  }
}
